apiVersion: '1.0'
name: python globals test
version: '0.1'

job:
  - task: test
    python: |
      m = 'Hello, {0}'
    outputs:
      - m
  - task: format
    python: |
      f_m = m.format(name)
    inputs:
      message: task.test.n
      name: 'Brian'
    outputs:
      - f_m