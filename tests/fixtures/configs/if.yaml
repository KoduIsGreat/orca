apiversion: orca/'1.0'

version: '0.1'

var:
  value: 5

job:
  - task: echo
    bash: echo hello

  - control: is the value equal to 5
    if: var.value == 5
    do:
      - task: conditional echo
        bash: echo wow we're in a condition!

  - task: last echo
    bash: echo done
