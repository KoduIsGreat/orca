apiVersion: '1.0'
name: python globals test
version: '0.1'
var:
 name: 'Brian'
job:
  - task: test
    python: |
      m = 'Hello, {0}'
    outputs:
      - m
  - task: format
    python: |
      f_m = m.format(name)
    inputs:
      message: task.tes.m
      name: var.nam
    outputs:
      - f_m