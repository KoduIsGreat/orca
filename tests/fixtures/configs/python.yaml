apiversion: orca/'1.0'

version: '0.1'
dependencies:
  - os
var:
  afile: 'a.py'
  d: 20
job:
  # python task
  - task: test
    python: var.afile
    outputs:
      - c

## test
  - task: test2
    python: var.afile
    outputs:
      - c

  - task: test3
    python: |
      a =  i1 + i2 + i3
    inputs:
      i1: task.test.c
      i2: task.test2.c
      i3: var.d
    outputs:
      - a
